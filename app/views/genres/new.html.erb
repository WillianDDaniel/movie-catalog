<section class='section-container align-center'>

  <h2 class='section-title'>Cadastrar novo Gênero</h2>

  <div class="error">
    <% if flash[:errors] %>
      <p>
        <%= flash[:errors] %>
      </p>
    <% end %>
  </div>

  <%= form_with model: @genre, html: { class: "form genre-form" } do |form| %>

    <div class="name">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>

    <div class="submit">
      <%= form.submit "Cadastrar", class: "submit-button main-button" %>
    </div>

  <% end %>
</section>
